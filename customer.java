public class customer{
    public customer() {
        // auto-generated constructor stub
    }

java.sql.Connection con = Connection.getInstance().getConnection();

/*
* View number of available vehicles for a specific type, location, and time interval.
* User can provide any subset of {car type, location, time interval} to view
* available vehicles. If user provides no information, application will return
* list of all vehicles (at that branch) sorted alphabetically.
*/

public Object[][] view(String keyword){
    // null
}

public boolean Reserve() {
    // null
}








}